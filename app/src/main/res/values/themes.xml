<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Biteful" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Biteful" parent="Base.Theme.Biteful" />
    <style name="CustomBottomNavStyle" parent="Widget.Material3.BottomNavigationView">
        <item name="materialThemeOverlay">@style/CustomBottomNavOverlay</item>
    </style>

    <style name="CustomBottomNavOverlay" parent="">
        <item name="colorSecondaryContainer">@color/biteful_orange_light</item>

        <item name="colorOnSecondaryContainer">@color/biteful_icon_black</item>
    </style>
</resources>